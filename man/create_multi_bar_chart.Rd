% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bar_charts.R
\name{create_multi_bar_chart}
\alias{create_multi_bar_chart}
\title{Créer un graphique en barres multiples avec des catégories}
\usage{
create_multi_bar_chart(
  data,
  group_var,
  value_vars,
  value_titles = NULL,
  colors = NULL,
  group_levels = NULL,
  main_title = NULL,
  subtitle = NULL,
  caption = NULL,
  y_axis_title = "Pourcentage",
  y_max = NULL,
  percent = TRUE,
  output_path = "plot.png",
  width = 16,
  height = 8,
  logo_path = NULL,
  logo_position = "bottomright",
  logo_width = 0.15,
  title_size = 14,
  subtitle_size = 12,
  caption_size = 10,
  bar_title_size = 14,
  axis_text_size = 12,
  axis_title_size = 12
)
}
\arguments{
\item{data}{Un data.frame contenant les données à visualiser}

\item{group_var}{Nom de la variable de regroupement (par ex. "party")}

\item{value_vars}{Vecteur de noms des variables de valeurs (par ex. c("never_pct", "sometimes_pct", "often_pct"))}

\item{value_titles}{Vecteur de titres pour chaque variable de valeur (même longueur que value_vars)}

\item{colors}{Vecteur nommé de couleurs pour chaque niveau de group_var}

\item{group_levels}{Vecteur optionnel pour définir l'ordre des niveaux de group_var}

\item{main_title}{Titre principal du graphique}

\item{subtitle}{Sous-titre du graphique}

\item{caption}{Légende du graphique}

\item{y_axis_title}{Titre de l'axe Y (apparaît uniquement sur le premier graphique)}

\item{y_max}{Valeur maximale de l'axe Y (si NULL, calculée automatiquement)}

\item{percent}{Booléen indiquant si les valeurs sont des pourcentages}

\item{output_path}{Chemin pour sauvegarder le graphique}

\item{width}{Largeur du graphique en pouces}

\item{height}{Hauteur du graphique en pouces}

\item{logo_path}{Chemin vers le logo à ajouter}

\item{logo_position}{Position du logo ("bottomright", "bottomleft", "topright", "topleft")}

\item{logo_width}{Largeur du logo en proportion de la largeur du graphique}

\item{title_size}{Taille du titre principal}

\item{subtitle_size}{Taille du sous-titre}

\item{caption_size}{Taille du texte de la légende}

\item{bar_title_size}{Taille des titres des barres}

\item{axis_text_size}{Taille du texte des axes}

\item{axis_title_size}{Taille du titre de l'axe Y}
}
\value{
Le graphique ggplot final
}
\description{
Cette fonction crée un graphique en barres multiples pour plusieurs catégories,
avec une mise en page propre et un style Datagotchi.
}
\examples{
\dontrun{
# Créer un dataframe de données
df <- data.frame(
  party = rep(c("PCC", "PLC", "BQ", "NPD", "PV"), 3),
  category = rep(c("never", "sometimes", "often"), each = 5),
  value = c(
    35, 25, 20, 15, 40,
    45, 50, 55, 50, 45,
    20, 25, 25, 35, 15
  )
)

# Convertir à un format plus large
df_wide <- tidyr::pivot_wider(
  df, 
  names_from = category, 
  values_from = value
)

# Définir les couleurs
party_colors <- c(
  "PCC" = "#1A4782",
  "PLC" = "#D71920",
  "BQ" = "#33B2CC", 
  "NPD" = "#F58220",
  "PV" = "#3D9B35"
)

# Créer le graphique
create_multi_bar_chart(
  data = df_wide,
  group_var = "party",
  value_vars = c("never", "sometimes", "often"),
  value_titles = c("JAMAIS", "PARFOIS", "SOUVENT"),
  colors = party_colors,
  main_title = "FRÉQUENCE DE VISITES AUX MUSÉES PAR AFFILIATION POLITIQUE",
  subtitle = "Pourcentage de partisans selon leur fréquence de visites aux musées",
  caption = "Source: Données simulées",
  percent = TRUE,
  output_path = "musee_barplot.png"
)
}

}
